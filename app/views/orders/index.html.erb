<div>
  <h3>My Orders</h3>
  <% @orders.each do |order| %>
    <div>
      <p>Order No: <%= order.id %>
      <%= link_to "See items for this order", order_path(order) %>
    </div>
  <% end %>
</div>